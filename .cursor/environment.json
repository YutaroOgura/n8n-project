{
  "setup_commands": [
    "echo 'Setting up n8n project environment'",
    "node --version",
    "npm --version",
    "pwd",
    "ls -la",
    "echo 'Environment setup completed'"
  ],
  "environment_variables": {
    "NODE_ENV": "development",
    "PROJECT_TYPE": "n8n-workflows",
    "PATH": "/usr/local/bin:/usr/bin:/bin"
  },
  "required_tools": ["node", "npm", "git"]
}
